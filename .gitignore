# --- 依存パッケージを確実に弾く ---
# 先頭にスラッシュを付けないことで、どこにあっても無視させます
vendor/
node_modules/

# 階層を明示して二重にブロック
src/vendor/
src/node_modules/

# --- Docker関連 (権限エラー対策) ---
docker/mysql/data/
docker/mysql/data/**

# --- 設定ファイル ---
.env
src/.env
